_schema-version: "3.2"
ID: ext-fiori-PlantoLog
version: 0.0.2
provider: SAP
modules:
- name: ext-PlantoLog-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: ext-PlantoLog-html5-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - sapb1PlantoLog.zip
      name: sapb1PlantoLog
      target-path: resources/
- name: sapb1PlantoLog
  type: html5
  path: PlantoLog
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: ext-PlantoLog-html5-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: ext-PlantoLog-html5-app-host-service
    service-plan: app-host
parameters:
  deploy_mode: b1-webclient
